// Entry point for frontend (TypeScript)
import React from "react";
import ReactDOM from "react-dom/client";

const App = () => <div>Whiteboard App</div>;

const root = ReactDOM.createRoot(document.getElementById("root")!);
root.render(<App />);
